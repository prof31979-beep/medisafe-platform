import type { Metadata, Viewport } from 'next'
import { Inter, JetBrains_Mono } from 'next/font/google'
import { Analytics } from '@vercel/analytics/next'
import './globals.css'

const inter = Inter({ 
  subsets: ["latin"],
  variable: "--font-inter"
});

const jetbrainsMono = JetBrains_Mono({ 
  subsets: ["latin"],
  variable: "--font-jetbrains-mono"
});

export const metadata: Metadata = {
  title: 'MEDISAFE | Smart Pharmaceutical Logistics Ecosystem',
  description: 'Intelligent pharmaceutical supply chain monitoring system ensuring safe transportation of medicines, vaccines, and medical supplies using IoT sensors and real-time monitoring.',
  generator: 'v0.app',
  keywords: ['pharmaceutical logistics', 'supply chain', 'IoT monitoring', 'cold chain', 'medical transport', 'vaccine tracking'],
}

export const viewport: Viewport = {
  themeColor: '#0a1628',
  width: 'device-width',
  initialScale: 1,
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en" className="dark">
      <body className={`${inter.variable} ${jetbrainsMono.variable} font-sans antialiased`}>
        {children}
        <Analytics />
      </body>
    </html>
  )
}